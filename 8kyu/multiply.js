//👀 DESCRIPTION
// This code does not execute properly. Try to figure out why.

//👨‍💻 SOLUTION
let action;
function multiply(a, b){
    action = (a * b);
    return (action);
}
multiply(2,5)